<% include partials/header%>

<div class="container">
    <div class="row">
        <div class = "col-md-12 mx-auto">
            <table class = "table  table-sm table-bordered table-hover">
                <thead>
                    <tr>
                        <th>ID Pedido</th>
                        <th>ID Cliente</th>
                        <th>Fecha de Pedido</th>
                        <th>Libros</th>
                        <th>Total</th>
                        <th>Acción</th>
                    </tr>
                </thead>
                <body>
                    <% for(var i = 0; i < deliveriesClient.length; i++) { %>
                        <tr>
                            <td><%= deliveriesClient[i]._id %></td>
                            <td><%= deliveriesClient[i].idClient %></td>
                            <td><%= deliveriesClient[i].order_date %></td>
                            <td><%= deliveriesClient[i].books.length %></td>
                            <td><%= deliveriesClient[i].total %></td>
                            <td>
                                <a href="/clientDeliveryDetails/<%= deliveriesClient[i]._id%>" class="btn btn-info">Ver</a>
                                <a href="/deleteDelivery/<%= deliveriesClient[i]._id%>" class="btn btn-danger">Eliminar</a>
                            </td>
                        </tr>
                    <% } %> 
                </body>
            </table>
        </div>
    </div>
</div>


<% include partials/footer%>