<% include partials/header%>

<div class="container">
    <div class="row">
        <div class = "col-md-7 mx-auto">
            <div class = "card">
                <div class = "card-body">
                    <form action="/agentProcessDelivery/<%= agentDelivery._id%>" method="POST">
                        <div class="form-group">
                            <label for="delivery_location">Ubicación</label>
                            <input type="text", name="delivery_location", value="<%= agentDelivery.delivery_location%>" class="form-control" required>
                        </div>
                        <div class="form-group">
                            <label for="delivery_date">Fecha de entrega</label>
                            <input type="text", name="delivery_date", value="<%= agentDelivery.delivery_date%>" class="form-control" required>
                        </div>
                        <div>
                            <button type="submit" class="btn btn-success btn-block">Procesar</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

<% include partials/footer%>